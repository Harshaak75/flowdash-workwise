"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.buildEmployeeReportHTML = buildEmployeeReportHTML;
function buildEmployeeReportHTML(report, snapshot, trendData) {
    return `
<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

  <style>
    /* Force single-page PDF */
    @page {
      size: A4;
      margin: 24mm;
    }

    body {
      font-family: Helvetica, Arial, sans-serif;
      color: #333;
      margin: 0;
      padding: 0;
    }

    /* ONE PAGE CONTAINER */
    .page {
      width: 100%;
      height: 100%;
      page-break-after: avoid;
      page-break-inside: avoid;
    }

    h1 {
      color: #2563eb;
      text-align: center;
      font-size: 22px;
      margin: 0;
    }

    .subtitle {
      text-align: center;
      color: #666;
      font-size: 13px;
      margin: 6px 0 20px;
    }

    .kpi-grid {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 12px;
      margin-bottom: 20px;
    }

    .kpi {
      border: 1px solid #e5e7eb;
      padding: 12px;
      border-radius: 8px;
      background: #f9fafb;
      text-align: center;
    }

    .kpi b {
      display: block;
      font-size: 11px;
      color: #6b7280;
      margin-bottom: 4px;
      text-transform: uppercase;
    }

    .kpi .value {
      font-size: 18px;
      font-weight: bold;
    }

    .chart-section {
      text-align: center;
      margin-top: 10px;
      page-break-inside: avoid;
    }

    h2 {
      font-size: 16px;
      margin-bottom: 10px;
      color: #374151;
    }

    /* HARD SIZE — NO RESPONSIVE */
    .chart-container {
      width: 260px;
      height: 260px;
      margin: 0 auto;
    }

    canvas {
      width: 260px !important;
      height: 260px !important;
    }

    footer {
      margin-top: 20px;
      text-align: center;
      font-size: 10px;
      color: #9ca3af;
      border-top: 1px solid #e5e7eb;
      padding-top: 8px;
    }
  </style>
</head>

<body>
  <div class="page">

    <h1>Employee Performance Report</h1>
    <div class="subtitle">
      Period: ${report.fromDate.toDateString()} — ${report.toDate.toDateString()}
    </div>

    <div class="kpi-grid">
      <div class="kpi">
        <b>Total Tasks</b>
        <div class="value">${snapshot.totalTasks}</div>
      </div>
      <div class="kpi">
        <b>Completed</b>
        <div class="value">${snapshot.completedTasks}</div>
      </div>
      <div class="kpi">
        <b>Completion Rate</b>
        <div class="value">${snapshot.completionRate}%</div>
      </div>
      <div class="kpi">
        <b>Productivity</b>
        <div class="value">${snapshot.productivityScore}</div>
      </div>
    </div>

    <div class="chart-section">
      <h2>Daily Task Completions</h2>
      <div class="chart-container">
        <canvas id="trendChart" width="260" height="260"></canvas>
      </div>
    </div>

    <footer>
      Generated by Flowdash Workwise • Confidential
    </footer>

  </div>

  <script>
    const ctx = document.getElementById('trendChart').getContext('2d');
    const trendData = ${JSON.stringify(trendData || [])};
    
    // Format dates to be shorter (e.g. "Oct 1")
    const labels = trendData.map(d => {
        const date = new Date(d.date);
        return date.toLocaleDateString('en-US', { month: 'short', day: 'numeric' });
    });
    const counts = trendData.map(d => d.count);

    new Chart(ctx, {
      type: 'bar', // Using Bar for daily trend looks good
      data: {
        labels: labels,
        datasets: [{
          label: 'Completed Tasks',
          data: counts,
          backgroundColor: '#3b82f6',
          borderRadius: 4,
          maxBarThickness: 40
        }]
      },
      options: {
        responsive: false,
        animation: false,
        maintainAspectRatio: false,
        scales: {
            y: {
                beginAtZero: true,
                ticks: { stepSize: 1 }
            },
            x: {
                grid: { display: false }
            }
        },
        plugins: {
          legend: { display: false }
        }
      }
    });
  </script>
</body>
</html>
`;
}
//# sourceMappingURL=employeeReportTemplate.js.map